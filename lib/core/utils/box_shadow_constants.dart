import 'package:flutter/material.dart';

class BoxShadowsConstants {
  static const lightBoxShadow = BoxShadow(
    color: Colors.black12,
    offset: Offset(0, 0), // Set the offset of the shadow
    blurRadius: 10.0, // Set the blur radius of the shadow
    spreadRadius: 0.0, // Set the spread radius of the shadow
  );
}
